apiVersion: v1
kind: Secret
metadata:
  name: {{ include "grafana-oss-team-sync.fullname" . }}-secret
  labels:
    {{- include "grafana-oss-team-sync.labels" . | nindent 4 }}
data:
{{- range $key, $val := .Values.secrets }}
{{- if $val}}
  {{ $key }}: {{ $val | b64enc }}
{{- end }}
{{- end }}